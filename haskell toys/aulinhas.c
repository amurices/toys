#include <stdio.h>

int main()
{
  int cancela = 0;
  cancela = 2;
  if (cancela == 2)
  {
    printf("Cancela está sendo aberta\n");
  }

  cancela = 0;

  if (cancela == 0)
  {
    printf("Cancela está sendo fechada\n");
  }

  return 0;
}
